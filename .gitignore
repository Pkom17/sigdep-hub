# Build artefacts
target/
*.class
*.jar
node_modules/
dist/
.vite/

# Side-checkout of sigdep-contracts done by the CI inside the workspace
# (actions/checkout refuses paths outside the workspace).
.sigdep-contracts/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Secrets — never commit. Use .env.example as a template.
.env
.env.local
.env.*.local

# TLS material — provided per-environment by the deploy pipeline.
infra/nginx/certs/

# Logs
*.log
